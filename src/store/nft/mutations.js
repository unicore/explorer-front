export function setMyObjects(state, objects) {
    state.my_objects = objects
}

export function setMarket(state, market) {
    state.market = market
}

export function setAllObjects(state, objects) {
    state.all_objects = objects
}

export function setRequests(state, requests) {
    state.requests = requests
}
