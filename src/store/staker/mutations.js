export function set_plans (state, plans) {
  state.plans = plans
}

export function set_limits (state, limits) {
  state.limits = limits
}

export function set_sos (state, sos) {
  state.stake_objects = sos
}